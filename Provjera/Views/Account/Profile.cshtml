@model Provjera.Models.ViewModel.ApplicationUserViewModel

@{
    ViewData["Title"] = "Profile";
}

<h4>Profile</h4>

<div class="row">
    <div class="col-4
">
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Id)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.FulName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.FulName)
            </dd>
        </dl>
    </div>
    <div class="col-8">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        View
                    </th>
                </tr>
            </thead>
            @foreach(var item in Model.ToDoLists)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @Html.ActionLink("Task Prieview", "GetAllTasks", new { id = item.Id }) |
                    </td>
                </tr>
            }
        </table>
    </div>
       
</div>


<div>
    @Html.ActionLink("Add ToDo List", "AddToDoList", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
